{
  "name": "people-tree",
  "version": "0.5.0",
  "description": "Obsidian plugin: interactive family tree from YAML frontmatter",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.21.0",
    "obsidian": "latest",
    "typescript": "^5.0.0"
  }
}
